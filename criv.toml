[vault]
docs = "docs"
adr = "adr"

[source]
roots = ["src", "lib", ".github/workflows", "Cargo.toml", "hk.pkl", "mise.toml", "README.md"]
exclude = ["**/target/**", "**/node_modules/**"]
languages = []

[index]
source = true
notes = "memory"
embeddings = false

[enforce]
stages = ["commit", "push", "ci"]

[obsidian]
plugin = true
