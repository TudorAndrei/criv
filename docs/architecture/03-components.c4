views {
  view components {
    title 'CLI components and editor adapters'
    include criv.cli.*
    include criv.renderer
    include criv.obsidianPlugin
    include criv.vscodeExtension
    include criv.vault
    include * -> *
  }
}
